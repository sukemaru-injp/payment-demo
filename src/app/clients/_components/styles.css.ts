import { style } from '@vanilla-extract/css';

export const clientContainer = style({
  display: "flex",
  gap: "10px",
});

export const name = style({
  fontWeight: "bold",
  fontSize: "1.1em",
  color: "#3ea832"
})